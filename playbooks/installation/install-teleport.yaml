---
- name: install core packages for virtual machines
  hosts: "{{ hosts }}"
  become: yes
  roles:
    - mdsketch.teleport
  vars:
    teleport_ssh_labels:
      - k: "access"
        v: "homelab-vm"
    teleport_auth_token: "{{ teleport_auth_token }}"
    teleport_ca_pin: "{{ teleport_ca_pin }}"
    teleport_auth_server: "49.12.193.92:3025"
    teleport_proxy_server: "49.12.193.92:3025"